# دليل تشغيل البوت على Render 🚀

لقد تم إصلاح مشكلة المجلد المفقود (`webapp/static`) التي كانت تسبب توقف التطبيق. الآن يمكنك اتباع الخطوات التالية لرفع وتشغيل البوت على منصة Render.

## الخطوات الأساسية

### 1. إنشاء خدمة ويب (Web Service)
- قم بتسجيل الدخول إلى [Render](https://render.com).
- اضغط على **New +** ثم اختر **Web Service**.
- اربط حساب GitHub الخاص بك واختر المستودع (Repository).

### 2. إعدادات التشغيل (Runtime Settings)
- **Runtime:** `Python 3`
- **Build Command:** `pip install -r requirements.txt`
- **Start Command:** `python main.py`

### 3. متغيرات البيئة (Environment Variables)
يجب إضافة المتغيرات التالية في قسم **Environment**:
- `BOT_TOKEN`: التوكن الخاص بالبوت من @BotFather.
- `OWNER_ID`: معرف التليجرام الخاص بك (الممالك للبوت).
- `WEBAPP_URL`: الرابط الذي سيمنحه لك Render. **يجب أن يبدأ بـ https**. (انظر الفقرة أدناه لمعرفة كيفية الحصول عليه).
- `DATABASE_URL`:
  - في حال استخدام SQLite (الوضع الافتراضي): `sqlite+aiosqlite:///./bot_database.db`
  - *ملاحظة:* إذا استخدمت SQLite على Render، سيتم مسح البيانات عند كل إعادة تشغيل للتطبيق ما لم تقم بإضافة "Render Disk".

### 4. تهيئة قاعدة البيانات لأول مرة
البوت يحتاج لإضافة "المالك" (Owner) لقاعدة البيانات ليتمكن من العمل. يمكنك القيام بذلك بطريقتين:

#### الطريقة الأولى (تلقائية):
تعديل سطر التشغيل (Start Command) في المرة الأولى فقط إلى:
`python setup_db.py && python main.py`
بعد نجاح التشغيل لأول مرة، يمكنك إعادته إلى `python main.py`.

#### الطريقة الثانية (يدوية):
استخدام الـ **Shell** في لوحة تحكم Render وتشغيل الأمر:
`python setup_db.py`

---

## كيف تجد رابط الـ WebApp وتضيفه؟
1. بعد إنشاء الخدمة على Render، ستجد في أعلى الصفحة رابطاً ينتهي بـ `.onrender.com` (مثال: `https://my-bot-app.onrender.com`).
2. قم بنسخ هذا الرابط.
3. اذهب إلى تبويب **Environment** في لوحة تحكم Render.
4. ابحث عن متغير `WEBAPP_URL` واضغط على زر التعديل (Edit).
5. قم بلصق الرابط الذي نسخته بدلاً من `https://your-webapp-url.com`.
6. اضغط **Save Changes**. سيقوم Render بإعادة تشغيل البوت تلقائياً وتفعيل الرابط الجديد.

## نصائح هامة
- **قاعدة البيانات:** Render يمسح ملفات SQLite عند كل تحديث للكود. للاحتفاظ بالبيانات بشكل دائم، يفضل استخدام **Render PostgreSQL** (مجاني لفترة محدودة) وتغيير `DATABASE_URL` إلى رابط قاعدة البيانات التي سيعطيك إياها Render.
- **رابط الـ WebApp:** تأكد من أن الرابط في `WEBAPP_URL` دقيق جداً لأنه يستخدم لفتح الواجهة البرمجية داخل تليجرام.

تم الآن تجهيز الكود ليعمل مباشرة بمجرد رفعه!
